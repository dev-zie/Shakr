import 'package:flutter/material.dart';

class ShakingScreen extends StatelessWidget {
  const ShakingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Shaking Screen'),
      ),
    );
  }
}