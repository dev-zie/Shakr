class AppDimensions {
  AppDimensions._();

  // Lottie animation sizes
  static const double splashLottieSize = 150.0;
  static const double shakeLottieSize = 300.0;
  static const double searchingLottieSize = 250.0;

  // Avatar sizes
  static const double conversationAvatarSize = 56.0;
  static const double profileDialogAvatarSize = 100.0;
  static const double chatHeaderAvatarSize = 36.0;
  static const double photoAvatarSize = 120.0;

  // Icon sizes
  static const double conversationIconSize = 28.0;
  static const double profileDialogIconSize = 44.0;
  static const double chatHeaderIconSize = 18.0;
  static const double cameraIconSize = 20.0;
  static const double genderButtonIconSize = 26.0;
  static const double emptyChatIconSize = 48.0;
  static const double vibeCardIconSize = 24.0;

  // Match timer
  static const double matchTimerSize = 120.0;
  static const double matchTimerStrokeWidth = 8.0;

  // Age pickers
  static const double agePickerHeight = 250.0;
  static const double agePickerItemExtent = 50.0;
  static const double ageWheelPickerHeight = 200.0;
  static const double ageWheelPickerItemExtent = 40.0;

  // Intro page indicators
  static const double indicatorHeight = 8.0;
  static const double indicatorInactiveWidth = 8.0;
  static const double indicatorActiveWidth = 24.0;

  // Profile photo editor
  static const double uploadingIndicatorSize = 20.0;
  static const double uploadingIndicatorPadding = 12.0;

  // Empty states
  static const double emptyChatsIconSize = 64.0;

  // Match
  static const double matchFoundIconSize = 40.0;

  // VibeChip
  static const double vibeChipAvatarIconSize = 18.0;

  // Photo step
  static const double photoAvatarPlaceholderIconSize = 50.0;

  // Conversation list divider
  static const double dividerIndent = 72.0;
}
